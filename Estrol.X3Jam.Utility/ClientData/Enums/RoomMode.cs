namespace Estrol.X3Jam.Utility.Data {
    public enum RoomMode : byte {
        Solo = 0x0,     // Wait
        VS = 0x1,       // Wait
        Unknown = 0x2,  // Couple?
        JAM = 0x3       // Playing
    }
}
